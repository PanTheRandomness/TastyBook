/*
var express = require("express");
var router = express.Router();

const ctrl = require("../controllers/emailController");

router.route("/api/test-email").get(ctrl.emailTest);

module.exports = router;
*/